import BookStoreContainer from '../Components/BookStoreContainer';
import NavBar from '../Components/NavBar';

const Home = () => (
  <>
    <NavBar />
    <BookStoreContainer />
  </>
);

export default Home;
